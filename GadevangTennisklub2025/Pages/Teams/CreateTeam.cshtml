@page
@model GadevangTennisklub2025.Pages.Teams.CreateTeamModel
@{
    ViewData["Title"] = "Create Team";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

<div class="d-flex flex-column justify-content-center align-items-center min-vh-100 p-3 bg-light">

    <h1 class="mb-4 text-center">Opræt Hold</h1>

    <div class="w-100" style="max-width: 500px;">
        <div class="card shadow-sm">
            <div class="card-body">

                <form method="post" onsubmit="return confirmAction();">

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">Navn</label>
                        <input asp-for="Name" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="MembershipType" class="form-label">Vælg medlemstype</label>
                        <select asp-for="MembershipType" class="form-select" asp-items="Model.MembershipOptions">
                            <option value="">-- vælg medlemstype --</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="TrainerId" class="form-label">Vælg Træner</label>
                        <select asp-for="TrainerId" class="form-select" asp-items="Model.TrainerOptions">
                            <option value="">-- Vælg Træner --</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="DayOfWeek" class="form-label">Dag På Ugen</label>
                        <select asp-for="DayOfWeek" class="form-select" asp-items="Model.DayOptions">
                            <option value="">-- Vælg en dag --</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="TimeOfDay" class="form-label">Tid På Dagen</label>
                        <input asp-for="TimeOfDay" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="Length" class="form-label">Længde I Timer</label>
                        <input asp-for="Length" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="MinMembers" class="form-label">Minimum Antal Medlemmer</label>
                        <input asp-for="MinMembers" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="MaxMembers" class="form-label">Maximum Antal Medlemmer</label>
                        <input asp-for="MaxMembers" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Beskrivelse</label>
                        <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                    </div>

                    <div class="d-grid" style="grid-template-columns: 2fr 1fr 2fr; display: grid; gap: 1rem; margin-top: 1.5rem;">
                        <button type="submit" asp-page-handler="Create" class="btn btn-primary">Opræt Hold</button>
                        <div></div> <!-- spacer -->
                        <button type="submit" asp-page-handler="Cancel" class="btn btn-secondary">Tilbage</button>
                    </div>

                </form>

                @foreach (string s in Model.Messages)
                {
                    <p class="text-danger mt-3">@s</p>
                }

            </div>
        </div>
        <!-- Extra bottom padding -->
        <div style="height: 80px;"></div>
    </div>

</div>

@section Scripts {
    <script>
        function confirmAction() {
            return confirm("Er du sikker på at du vil oprætte holdet?");
        }
    </script>
}
